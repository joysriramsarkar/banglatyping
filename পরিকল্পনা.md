হ্যাঁ। এবার SEO পুরো বাদ দিয়ে শুধু **অ্যাপের ভিতরের product, typing engine, lesson system, practice system, data, bugs এবং games** নিয়ে বলছি।

আমি `main` branch-এর বর্তমান code দেখে বলছি; runtime-এ নিজে browser খুলে সব flow চালাইনি। তাই নিচে **কোডে নিশ্চিত দেখা সমস্যা** আর **উন্নতির architecture** আলাদা করে দিচ্ছি।

সবচেয়ে আগে আমার সরাসরি মূল্যায়ন:

> **তোমার অ্যাপ এখন আর prototype নয়। Core product-এর বেশিরভাগ কাঠামো তৈরি। কিন্তু একই কাজের ৩–৪টা আলাদা implementation পাশাপাশি চলছে। এই duplication-টাই এখন সবচেয়ে বড় technical debt।**

বর্তমানে structured curriculum-এ ১৩ level, ১৭ module, ৬১ lesson, ১৩০ section রয়েছে। পুরনো `lessons.ts`-এ আবার আলাদা legacy lesson system-ও আছে।

---

# ১. “ফুলপ্রুফ” করতে প্রথম কাজ: একটাই Typing Engine

এখন তোমার project-এ অন্তত এই কয়েকটা typing path আছে:

```text
LessonPlayer
useTypingPractice
VisualTypingDrill
useWordDrill
useTypingSession
game-specific input handlers
```

এটাই প্রথমে বদলাতে হবে।

## Final architecture

```text
                 ┌─────────────────────┐
                 │  Typing Core Engine │
                 └──────────┬──────────┘
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
       Lessons           Test             Games
          │                 │                 │
      Practice          Exam Mode       Arcade Mode
```

সবাই একই:

```text
Normalizer
Grapheme tokenizer
Comparator
Layout resolver
Error classifier
Timer
Metrics
Event recorder
```

ব্যবহার করবে।

---

# ২. Bengali Unicode handling এখনও পুরোপুরি safe নয়

এটা তোমার project-এর সবচেয়ে গুরুত্বপূর্ণ বিষয়।

তুমি ইতিমধ্যে `Intl.Segmenter('bn-IN', { granularity: 'grapheme' })` ব্যবহার করছ, normalization করছ, conjunct handling করছ—এটা সঠিক দিক।

কিন্তু অন্য জায়গাগুলো এখনও:

```ts
word.length
text[i]
text.slice(...)
Array.from(text).slice(...)
```

এর ওপর নির্ভর করছে।

## উদাহরণ

```text
ক্ষ
```

এটা একটি visual grapheme হলেও raw representation একাধিক code point হতে পারে।

তাই:

```ts
"ক্ষ".length
```

এবং:

```ts
bengaliSegmenter.graphemeLength("ক্ষ")
```

এক জিনিস নয়।

---

# ৩. নিশ্চিত bug: Legacy typing stats Bengali grapheme ভেঙে গুনছে

`use-typing-practice.ts`-এ:

```ts
const expectedLength = expectedWord.length;
const typedLength = typedWord.length;

for (let j = 0; j < Math.max(expectedLength, typedLength); j++) {
    const expectedChar = expectedWord[j] || '';
    const typedChar = typedWord[j] || '';
}
```

এটা grapheme-aware নয়।

ফলে:

```text
কি
ক্ষ
জ্ঞ
ত্র
শ্র
```

এর accuracy/error calculation ভুল হতে পারে।

### ঠিক পদ্ধতি

```ts
const expected = segmentGraphemes(expectedWord);
const actual = segmentGraphemes(typedWord);
```

তারপর:

```ts
compareGrapheme(expected[i], actual[i])
```

---

# ৪. Backspace-এও একই সমস্যা

এখন বিভিন্ন জায়গায়:

```ts
Array.from(currentInput).slice(0, -1)
```

বা:

```ts
slice(0, -1)
```

ব্যবহার হচ্ছে।

Bengali input-এ এটা সবসময় logical typing unit নয়।

Backspace-এর ideal behavior:

```text
ক্ষ
↓
ক্
↓
ক
↓
empty
```

অথবা layout-specific composition অনুযায়ী logical undo।

অর্থাৎ raw Unicode deletion নয়।

---

# ৫. সবচেয়ে বড় architectural bug: layout abstraction থাকলেও fallback hardcoded

তোমার `keyboard-layouts.ts`-এ এখন:

```text
banglaword
khipro
probhat
bijoy
avro
unijoy
```

এর configuration আছে।

কিন্তু `LessonPlayer`-এর fallback input mapping-এ সরাসরি:

```ts
getKeyboardLayoutConfig("banglaword")
```

ব্যবহার হচ্ছে।

অর্থাৎ ব্যবহারকারী অন্য layout নির্বাচন করলেও fallback physical-key mapping BanglaWord ধরে নিতে পারে।

এটা গুরুতর।

## ঠিক architecture

```ts
handlePhysicalKey(
    code,
    shift,
    activeLayout
)
```

এবং:

```ts
resolveProducedCharacter(
    activeLayout,
    code,
    modifiers
)
```

সব input path-এ একই function ব্যবহার করবে।

---

# ৬. `findKeyInfoForChar()`-এর জন্য layout explicit করো

এখন কিছু জায়গায়:

```ts
findKeyInfoForChar(nextCharToType)
```

ব্যবহার হচ্ছে।

Better:

```ts
findKeyInfoForChar(nextCharToType, activeLayout)
```

সবসময়।

কারণ:

```text
একই বাংলা character
→ layout ভেদে আলাদা key
```

হতে পারে।

---

# ৭. `getTypingHint()` এখনও placeholder

`src/lib/typing/engine.ts`-এ `getTypingHint()`-এর implementation এখনও placeholder এবং empty array return করে।

এটা সরাসরি অসম্পূর্ণ feature।

অর্থাৎ engine বলছে:

> typing hint supported

কিন্তু actual layout-specific sequence resolve করছে না।

এটাকে এমন হতে হবে:

```ts
getTypingHint("ক্ষ", "banglaword")
```

→

```text
Q
```

অথবা layout অনুযায়ী relevant sequence।

এবং:

```ts
getTypingHint("ক্ত", "bijoy")
```

→

```text
...
```

---

# ৮. `useTypingSession()` বানিয়েছ, কিন্তু এটাকেই canonical করা দরকার

তোমার নতুন `useTypingSession` + `sessionReducer` + `metrics` architecture আছে।

এটা ভালো।

কিন্তু code search-এ দেখা যাচ্ছে production UI-এর অনেক অংশ এখনও পুরনো engine ব্যবহার করছে; `useTypingSession` মূলত আলাদা নতুন engine হিসেবে পড়ে আছে।

এটাই এখন বদলাতে হবে।

### লক্ষ্য

```text
useTypingPractice       ❌ retire
useWordDrill            ❌ retire
VisualTypingDrill logic ❌ retire
LessonPlayer logic      ❌ simplify

                     ↓

                 useTypingSession
```

UI-specific adapter থাকতে পারে, কিন্তু typing logic আলাদা থাকবে।

---

# ৯. নতুন Session Engine-এ Backspace-ও bug আছে

`sessionReducer`-এ:

```ts
if (state.currentGraphemeIndex === 0) return state;
```

আর last event-কে corrected করে previous grapheme-এ ফেরে।

সমস্যা হলো:

ধরো target:

```text
বাংলা
```

user wrong input দিল, কিন্তু wrong input-এ `currentGraphemeIndex` এগোয়নি।

তারপর Backspace চাপল।

`currentGraphemeIndex` ইতিমধ্যেই একই জায়গায়।

তাই Backspace সেই wrong event-এর input buffer undo করার সঠিক mechanism পায় না।

এটা proper edit model দিয়ে ঠিক করতে হবে।

---

# ১০. Composition handling এখনও পুরোপুরি integrated নয়

Session engine-এ:

```text
COMPOSITION_START
COMPOSITION_UPDATE
COMPOSITION_END
```

আছে।

কিন্তু hook-এর event flow-এ composition start/update-এর canonical wiring অসম্পূর্ণ।

তাই:

```text
OS Bengali IME
Avro
BanglaWord dead key
browser composition
```

সব ক্ষেত্রে একই deterministic path দরকার।

---

# ১১. `TypingEvent`-এর semantic বদলাতে হবে

এখন তুমি event-এ:

```ts
expectedGrapheme
actualInput
correct
latencyMs
layout
```

রাখছ।

ভালো।

কিন্তু `"actualInput"`-কে সবসময় grapheme ভাবলে আবার সমস্যা হবে।

আমি event-কে দুই স্তরে ভাঙতাম:

```ts
interface KeystrokeEvent {
  keyCode: string;
  key: string;
  shift: boolean;
  timestamp: number;
}
```

এবং:

```ts
interface GraphemeEvent {
  expected: string;
  produced: string;
  sequence: string[];
  correct: boolean;
  errorType: ErrorType | null;
}
```

এতে তুমি বলতে পারবে:

> “ক্ষ ভুল হয়েছে”

এবং:

> “ক্ষ টাইপ করার সময় ৩টি physical key-এর মধ্যে ২য় key-তে ভুল হয়েছে।”

---

# ১২. Metrics-এ বড় semantic সমস্যা আছে

`metrics.ts`-এ:

```ts
const inputEvents = events.filter(e => e.actualInput !== '');
const totalKeystrokes = inputEvents.length;
```

কিন্তু event = grapheme event হলে:

```text
১ grapheme ≠ ১ keystroke
```

উদাহরণ:

```text
ক
```

এক রকম।

কিন্তু:

```text
ক্ষ্ম
```

অনেক physical stroke লাগতে পারে।

তাই:

```text
GPM
```

এবং:

```text
KPM / SPM
```

দুটোর source আলাদা হওয়া উচিত।

### Final metrics

```text
GPM  = correct graphemes / minute
KPM  = physical keystrokes / minute
CPM  = code points / minute
WPM  = standardized typing metric
```

এগুলোকে আর একে অপরের proxy বানিও না।

---

# ১৩. WPM-এর “GPM / 4” বাদ দাও

LessonPlayer-এ:

```ts
const wpm = Math.round(gpm / 4);
```

এটা একটি heuristic, standard WPM নয়।

User-কে:

> আনুমানিক WPM

বলতে পারো, কিন্তু serious test-এ এটি authoritative metric হওয়া উচিত নয়।

বাংলার জন্য আমি UI-তে প্রধান করতাম:

```text
গতি
৫২ GPM
```

তার নিচে:

```text
WPM (standardized)
```

এবং:

```text
94% accuracy
```

---

# ১৪. Accuracy আরও পরিষ্কারভাবে আলাদা করো

এখন corrected error বাদ দিয়ে accuracy calculate করলে user:

```text
অনেক ভুল করেছে
↓
Backspace করে সব ঠিক করেছে
↓
accuracy 99%
```

পেতে পারে।

এটা ভুল নয়, কিন্তু metric-এর নাম হওয়া উচিত:

```text
Final Accuracy
```

আর দেখাও:

```text
Raw Accuracy
Corrected Errors
Uncorrected Errors
```

যেমন:

```text
Final accuracy       98.2%
Raw accuracy         92.5%
Corrected errors      7
Uncorrected errors    2
```

এতে বাস্তব skill বোঝা যাবে।

---

# ১৫. Consistency metric-ও আরও rigorous করা দরকার

বর্তমান algorithm ৫-second windows নিয়ে CV বের করে।

এতে short session বা sparse session-এর ক্ষেত্রে odd result হতে পারে।

Better:

```text
5-second GPM series
→ ignore windows with insufficient samples
→ median
→ MAD / standard deviation
→ consistency score
```

আর user-কে raw graph-ও দাও।

---

# ১৬. সবচেয়ে বড় lesson bug: একবার pass করলেই mastery

বর্তমান `recordLessonCompletion()`:

```ts
const isCompleted = accuracy >= 90;
const isMastered = accuracy >= 97;
```

অর্থাৎ একবার 97% হলেই:

```text
MASTERED
```

হয়ে যাচ্ছে।

তোমার own type system-এ লেখা আছে:

> MASTERED = >= 97% accuracy across 3+ sessions

কিন্তু engine সেটা enforce করছে না।

এটা সরাসরি inconsistency।

### ঠিক rule

```text
97%+
3 successful sessions
minimum sample size
recent accuracy stable
```

তারপর:

```text
MASTERED
```

---

# ১৭. Lesson navigation দিয়ে section bypass করা যাচ্ছে

LessonPlayer-এর section navigation:

```text
Section 1
Section 2
Section 3
Section 4
```

যেকোনো pill click করে `initSection(idx)` করা যায়।

অর্থাৎ user সরাসরি:

> mastery section

এ চলে যেতে পারে।

### Rule হওয়া উচিত

```text
Current section → clickable
Completed section → clickable
Future section → locked
```

এটা খুব গুরুত্বপূর্ণ।

---

# ১৮. Lesson completion data ভুল

Lesson-এর শেষে:

```ts
recordLessonCompletion(
  state,
  lesson.id,
  displayAccuracy,
  displayWpm,
  displayGpm
)
```

শুধু **শেষ section-এর metrics** পাঠানো হচ্ছে।

অর্থাৎ:

```text
Section 1 → 92%
Section 2 → 94%
Section 3 → 90%
Section 4 → 99%
```

পুরো lesson-এর result হিসেবে 99% চলে যেতে পারে।

এটা lesson-level score নয়।

### ঠিক model

```text
lessonStats = aggregate(sectionStats[])
```

যেমন:

```text
total attempts
total correct
total errors
total duration
total graphemes
```

তারপর weighted accuracy:

```text
totalCorrect / totalAttempts
```

---

# ১৯. একই কারণে DB-তে lesson error data ভুল

LessonPlayer-এর final API call-এ:

```ts
erredCharacters: []
```

পাঠানো হচ্ছে।

অর্থাৎ modern curriculum-এর সবচেয়ে মূল্যবান error data-ই database-এ যাচ্ছে না।

তাহলে তোমার:

> Adaptive Mistake Hub

পুরোপুরি accurate হবে কীভাবে?

প্রতিটি section-এর event/aggregated error থেকে:

```text
expected grapheme
error count
error type
```

পাঠাতে হবে।

---

# ২০. DB schema বনাম বর্তমান types-এ mismatch আছে

বর্তমান curriculum type-এ:

```ts
bestGpm
```

আছে।

কিন্তু পুরনো database schema-র `user_lesson_completion`-এ `best_wpm`, `best_accuracy` থাকলেও `best_gpm` নেই।

এখন schema migration করে model-গুলো align করতে হবে।

---

# ২১. LocalStorage-কে progression-এর primary source বানিও না

বর্তমানে curriculum state:

```text
localStorage
```

এর ওপর নির্ভর করছে।

এতে:

```text
Chrome → Firefox
Laptop → phone
Incognito → normal
browser reset
```

হলে progress হারিয়ে যেতে পারে।

Guest mode-এ localStorage ভালো।

কিন্তু logged-in user-এর জন্য:

```text
local cache
      ↕
server state
```

sync হতে হবে।

---

# ২২. Server security আরও শক্ত করতে হবে

`/api/user-progress` user authentication verify করছে, userId ownership-ও check করছে। এটা ভালো।

কিন্তু client:

```json
{
  "wpm": 999,
  "accuracy": 100,
  "errors": 0
}
```

এরকম result পাঠাতে পারে।

API মূলত সেই metrics-গুলো trust করছে।

তাই:

### সাধারণ progress

client metrics accept করা যায়।

### Certificate / leaderboard / official exam

client metric trust করা যাবে না।

এখানে:

```text
session id
server-issued challenge
test text/version
duration
events/hash
```

এর মতো verification model দরকার।

---

# ২৩. Certificate-এর ক্ষেত্রেও একই

যদি certificate “verified” হয়, তাহলে:

```text
client says:
I got 72 WPM
```

এটা যথেষ্ট নয়।

Certificate record server-side তৈরি/সংরক্ষণ হওয়া উচিত।

```text
certificate_id
user_id
test_id
text_version
final_metrics
issued_at
verification_hash
```

---

# ২৪. `useTimer` আরও robust হওয়া দরকার

`start()` বারবার call হলে নতুন interval তৈরি করার সুযোগ আছে।

Production timer-এ:

```text
status = idle
active
paused
finished
```

এই state machine রাখো।

আর time:

```ts
performance.now()
```

থেকে derive করো।

`setInterval(1000)` শুধু display update-এর জন্য থাকুক।

---

# ২৫. সবচেয়ে বড় practice system upgrade

এখন তোমার content আছে।

এখন দরকার **Practice Engine**।

আমি ৭টা mode দিতাম:

### Learn

নতুন skill।

### Precision

accuracy priority।

### Repetition

একই weak pattern বারবার।

### Speed Burst

৩০–৬০ সেকেন্ড।

### Endurance

৩–৫ মিনিট।

### Mistake Repair

শুধু user-এর ভুল।

### Exam

কোনো hint নয়।

---

# ২৬. Practice generator-এর formula

ধরো user-এর data:

```text
ক্ষ   61%
জ্ঞ   69%
ত্র   78%
ি    92%
ক    99%
```

পরের session:

```text
40% = weak
25% = adjacent skill
20% = mixed known
10% = new
5%  = confidence check
```

যেমন:

```text
ক্ষ ক্ষ জ্ঞ ক্ষ
ক্ষ্ম রক্ষ
ত্র ক্র শ্র
বিক্ষিপ্ত
শিক্ষার্থী
```

এতে random practice-এর বদলে intelligent practice হবে।

---

# ২৭. শুধু accuracy নয়—latency-ও track করো

একজন user:

```text
ক্ষ = 98%
```

কিন্তু:

```text
average latency = 1100ms
```

অন্যজন:

```text
ক্ষ = 96%
latency = 320ms
```

দ্বিতীয়জন বেশি fluent।

তাই mastery model:

```text
accuracy
+
latency
+
consistency
+
repetition stability
```

এর ওপর হওয়া উচিত।

---

# ২৮. Transition difficulty মাপো

এটা খুব powerful।

ধরো:

```text
ক → ল = 180ms
ক → ি = 220ms
ক → ্ = 190ms
স → ্ = 510ms
```

তাহলে system বুঝবে:

> `স্` transition সমস্যা।

তারপর targeted drill:

```text
স্
স্ স্
স্ ত
স্ ক
স্ প
```

এটা সাধারণ typing app-এ থাকে না।

---

# ২৯. Practice content কীভাবে বাড়াবে

এখনকার word bank-কে তিন স্তরে নাও।

## স্তর ১ — controlled

একটি skill-এর allowed characters দিয়ে শব্দ।

যেমন Home Row।

## স্তর ২ — semi-controlled

৮০% known + ২০% নতুন।

## স্তর ৩ — natural Bengali

বাস্তব শব্দ।

---

# ৩০. Word bank অনেক বড় করো

বর্তমান codebase-এ game এবং lesson word pool আছে, কিন্তু production-level trainer-এর জন্য অনেক বড় corpus দরকার।

আমি target রাখতাম:

```text
1000+ common words
3000+ long-tail words
500+ phrases
500+ sentences
100+ paragraphs
```

এগুলো manually random নয়—metadata সহ।

যেমন:

```ts
{
  word: "প্রযুক্তি",
  frequency: 0.82,
  difficulty: 4.8,
  graphemeCount: 5,
  hasKar: true,
  hasConjunct: true,
  phola: ["র-ফলা"],
  skills: ["conjunct", "ra-phola"]
}
```

---

# ৩১. প্রতিটি Bengali grapheme-কে skill tag দাও

যেমন:

```text
ক্ষ
```

এর metadata:

```text
conjunct
2 consonants
hasanta
banglaword-direct
high-difficulty
```

আর:

```text
কি
```

হবে:

```text
consonant
i-kar
2-step
```

এতে content generator অনেক শক্তিশালী হবে।

---

# ৩২. `ensureSpacedDrillItems()`-এর বর্তমান ধারণা বদলাও

এখন system কিছু drill-এর মধ্যে নিজে space ঢুকিয়ে দেয়।

এটা carefully curated lesson-এ undesirable হতে পারে।

কারণ content:

```text
A
B
C
D
```

কে auto:

```text
A B C D
```

বানানো হচ্ছে।

Lesson data-তে explicit metadata দাও:

```ts
spacing: {
  enabled: true,
  interval: 3
}
```

অথবা:

```ts
spacing: "none"
```

অথবা:

```ts
spacing: "natural"
```

---

# ৩৩. Practice lesson-এর ideal structure

প্রতিটি lesson:

```text
১. Explain
২. Demonstrate
৩. Isolated practice
৪. Pattern practice
৫. Word practice
৬. Mixed practice
৭. Precision check
৮. Mastery test
৯. Review recommendation
```

তোমার `LessonSection` type ইতিমধ্যেই এ ধরনের section support করে।

এখন শুধু engine-টা আরও শক্ত করতে হবে।

---

# ৩৪. Home/Top/Bottom row এখন কীভাবে practice বাড়াবে

তোমার ৭-stage row lessons ইতিমধ্যেই আছে।

এগুলোর প্রতিটির ভিতরে আরও content variation:

```text
Round A
isolated

Round B
alternation

Round C
ascending

Round D
descending

Round E
random

Round F
word

Round G
timed
```

একই skill-এ ২০টা exercise থাকবে, কিন্তু repeated content নয়।

---

# ৩৫. “Warm-up” যোগ করো

Practice শুরু হওয়ার আগে:

```text
আজকের দুর্বলতা:
ক্ষ
জ্ঞ
ৌ
```

তারপর ৩০ সেকেন্ড:

```text
ক্ষ ক্ষ জ্ঞ ক্ষ
ৌ ৌ ক্ষ জ্ঞ
```

তারপর মূল lesson।

---

# ৩৬. “Cool-down” যোগ করো

Session শেষে:

```text
আজকের ফল

GPM ↑
Accuracy ↑

তবু সমস্যা:
জ্ঞ
র-ফলা

পরের practice:
৪ মিনিট
```

এতে session closure হয়।

---

# ৩৭. Daily training algorithm

আমি default session দিতাম:

```text
02:00 Warmup
03:00 Weakness drill
02:00 Precision
02:00 Speed
01:00 Test
```

মোট:

```text
10 মিনিট
```

User পরে নিজের duration বদলাতে পারবে।

---

# ৩৮. এখন games নিয়ে আসি

বর্তমান arcade-এ তিনটি game আছে:

```text
Falling Words
Space Defender
Speed Racer
```

এবং localStorage high score আছে।

Games দেখতে feature-rich হয়েছে।

কিন্তু **typing trainer হিসেবে এগুলো এখনও core typing engine থেকে বিচ্ছিন্ন।**

এটাই পরিবর্তন করতে হবে।

---

# ৩৯. Game principle

Game যেন শুধু:

> “বাংলা word type করো”

না হয়।

Game যেন বলে:

> “এই skill practice করো—কিন্তু game-এর মধ্যে।”

---

# ৪০. Falling Words-কে কীভাবে উন্নত করবে

বর্তমান game-এ:

* falling words
* lives
* combo
* level
* freeze
* bomb
* heart
* high score

আছে।

কিন্তু কিছু সমস্যা আছে।

### Bug ১ — multiple word এক frame-এ পড়লে এক life-ই কমে

কারণ `lostLife` boolean।

দুটি word একই frame-এ ground hit করলে:

```text
damage = 2
```

হওয়া উচিত।

---

### Bug ২ — score `.length` দিয়ে

Complex Bengali word-এ code-unit length score bias তৈরি করতে পারে।

এখানে:

```ts
segmentGraphemes(word).length
```

ব্যবহার করো।

---

### Bug ৩ — target lock নেই

একাধিক word যদি একই prefix share করে:

```text
প্রকৃতি
প্রযুক্তি
```

user-এর target switching সমস্যা হতে পারে।

Input শুরু হলে:

```text
targetWordId
```

lock করো।

---

### Bug ৪ — accuracy metric নেই

Game result-এ দেখাও:

```text
GPM
Accuracy
Mistakes
Missed words
Max combo
```

---

# ৪১. Falling Words-এর নতুন mode

### Classic

বর্তমান style।

### Precision

ভুল করলে combo reset।

### Weakness Rain

শুধু user's weak words।

### Conjunct Storm

শুধু:

```text
ক্ষ
জ্ঞ
ত্র
শ্র
```

যুক্ত শব্দ।

### Endless

Level শেষ নেই।

### 60-second Sprint

এক মিনিটে যত বেশি।

---

# ৪২. Falling Words powerup-কে আরও meaningful করো

এখন powerup random flag করা হয়।

Better:

### Freeze

১০টি consecutive correct।

### Bomb

একটি difficult word perfect।

### Heart

৯৮% accuracy বজায় রেখে ১৫ শব্দ।

অর্থাৎ powerup হবে:

> **skill reward**

random lottery নয়।

---

# ৪৩. Space Defender-এর সবচেয়ে বড় bug

এখানে main animation effect-এর dependency-তে `enemies` আছে।

```text
enemies
```

প্রতি animation frame-এ পরিবর্তিত হচ্ছে।

ফলে effect বারবার teardown/restart হতে পারে।

এটা performance-এর জন্য খুব খারাপ architecture।

### ঠিক পদ্ধতি

```text
RAF একবার শুরু
       ↓
refs/reducer দিয়ে game state
       ↓
render state আলাদাভাবে update
```

`enemies` dependency থেকে সরিয়ে refs ব্যবহার করো।

---

# ৪৪. Space Defender-এ আরও একটি bug

Game over-এর সময়:

```text
score
shipsDestroyed
wave
```

closure value stale হতে পারে।

ফলে final result save করার সময় পুরনো value লেখা পড়তে পারে।

এটার জন্য:

```ts
scoreRef.current
shipsDestroyedRef.current
```

অথবা central reducer ব্যবহার করো।

---

# ৪৫. Space Defender-এর target system উন্নত করো

বর্তমানে নতুন input-এর prefix অনুযায়ী enemy select করা হয়।

Better:

```text
first valid match
↓
target locked
↓
backspace allowed
↓
target complete
↓
next target
```

User একটি ship শুরু করার পরে অন্য ship-এ হঠাৎ switch করবে না।

---

# ৪৬. Space Defender-কে typing lesson বানানো যায়

Enemy category:

```text
Scout
→ common word

Elite
→ kar-heavy word

Commander
→ phola

Boss
→ conjunct
```

Boss:

```text
ক্ষ্ম
ন্ত্র
ষ্ট্র
```

থেকে তৈরি শব্দ দেবে।

তাহলে difficulty narrative-ও থাকবে, skill practice-ও হবে।

---

# ৪৭. Boss mechanic

ধরো Boss:

> **প্রযুক্তি**

Boss health:

```text
████
```

প্রতিটি correctly typed difficult segment:

```text
প্র
↓
প্রযুক্তি
```

health কমাবে।

ভুল হলে:

```text
Boss attack
```

এতে একটি শব্দকে multi-stage typing challenge করা যায়।

---

# ৪৮. Speed Racer-এর বড় সমস্যা

বর্তমানে AI cars fixed base WPM:

```text
28
34
24
```

এবং race progression targetText-এর `.length` দিয়ে হিসাব হচ্ছে।

অর্থাৎ Bengali grapheme অনুযায়ী race distance সঠিক নয়।

এটা:

```ts
segmentGraphemes(targetText).length
```

এর ওপর হওয়া উচিত।

---

# ৪৯. Racer-এ actual typing error meaningful করো

বর্তমানে target prefix না মিললে শুধু error sound হয়।

কিন্তু car-এর ওপর effect নেই।

Better:

```text
Correct → accelerate
Wrong → small brake
Backspace correction → recovery
Perfect streak → boost
```

---

# ৫০. Racer-এর নতুন mechanics

### Nitro

শুধু 35 WPM নয়।

বরং:

```text
5 perfect graphemes → +nitro
10 → big nitro
20 → overtake
```

### Slipstream

টানা নির্ভুল typing।

### Crash

তিনটি consecutive error।

### Pit stop

pause > 2 seconds।

এতে speed + consistency দুটোই গুরুত্বপূর্ণ হয়।

---

# ৫১. Racer-এর Ghost Mode

এটা অত্যন্ত ভালো feature হবে।

User personal best করেছে:

```text
51 GPM
```

পরেরবার:

```text
👻 আপনার আগের রেকর্ড
```

দৌড়াবে।

User দেখবে:

> আমি নিজের ২০ সেকেন্ড আগের performance-এর চেয়ে কোথায় এগিয়ে/পিছিয়ে।

Online leaderboard না থাকলেও এটা খুব ভালো game loop।

---

# ৫২. Racer-এর difficulty

```text
Beginner AI
20 GPM

Normal
30 GPM

Hard
40 GPM

Expert
dynamic
```

কিন্তু AI speed fixed না হয়ে slight randomness + controlled challenge রাখো।

---

# ৫৩. তিনটি game-এই একই game typing engine

```text
GameTypingController
```

থাকবে:

```ts
start()
input()
backspace()
finish()
getMetrics()
getErrors()
```

তারপর:

```text
FallingGame
SpaceGame
RacerGame
```

নিজের visual/game mechanics চালাবে।

Typing logic আবার লিখবে না।

---

# ৫৪. Games-এর data model-ও উন্নত করো

এখন localStorage stats:

```text
falling highScore
space highScore
racer bestWpm
```

আছে।

আরও দরকার:

```text
gamesPlayed
bestScore
bestGpm
bestAccuracy
bestCombo
averageSession
weakSkills
preferredDifficulty
```

---

# ৫৫. `totalPlayTimeMinutes` dead field

`game-storage.ts`-এ:

```ts
totalPlayTimeMinutes
```

আছে।

কিন্তু এটা increment হচ্ছে না।

দুইটি পথ:

```text
বাস্তবে track করো
```

অথবা:

```text
field সরিয়ে দাও
```

Dead data রাখবে না।

---

# ৫৬. Custom Drill-এর নিশ্চিত bug

`useWordDrill` ও `VisualTypingDrill`-এ logic এমন ধরনের:

```ts
setDrills(customDrills);
resetDrill();
```

কিন্তু `resetDrill()` আবার:

```ts
setDrills(initialDrills);
```

দিচ্ছে।

অর্থাৎ custom drill set করার পর reset আবার পুরনো drills বসিয়ে দিতে পারে।

এই কারণেই:

> **“Practice My Mistakes”**
>
> পুরোপুরি reliable নয়।

এটা P0/P1 bug হিসেবে ঠিক করো।

সঠিক pattern:

```ts
resetDrill(customDrills)
```

অথবা:

```ts
setDrills(customDrills);
resetStateOnly();
```

---

# ৫৭. WPM history-তে stale closure-এর সমস্যা আছে

`startDrill()`-এর interval closure-এর মধ্যে:

```ts
totalCharsTyped
```

ধরা হচ্ছে।

পরে totalCharsTyped বাড়লেও interval callback পুরনো value capture করে রাখতে পারে।

ফলে ৩০-second history inaccurate হওয়ার সম্ভাবনা আছে।

### ঠিক পদ্ধতি

```ts
totalCharsTypedRef.current
```

অথবা:

```text
central session state
```

---

# ৫৮. Game word pool অনেক বড় করো

বর্তমানে game dictionary আছে:

```text
easy
medium
hard
conjunct
```

এটা ভালো শুরু।

কিন্তু game-এ ২০ বার:

```text
বাংলাদেশ
প্রযুক্তি
স্বাধীনতা
```

ঘুরতে থাকবে—এটা চাই না।

আমি game content:

```text
500 easy
500 medium
500 hard
300 conjunct
200 phrase
100 sentence
```

এর দিকে নিতাম।

---

# ৫৯. Game word metadata

প্রতিটি word:

```ts
{
  text: "প্রযুক্তি",
  level: 4,
  graphemes: 5,
  difficulty: 4.7,
  skills: ["ra-phola", "conjunct", "u-kar"],
  frequency: "common",
}
```

তাহলে game dynamic হতে পারবে।

---

# ৬০. সবচেয়ে গুরুত্বপূর্ণ—Game আর Practice-এর মধ্যে data loop

এখন:

```text
Practice
```

একটা জিনিস।

আর:

```text
Game
```

অন্য জিনিস।

এগুলো জুড়ো:

```text
Practice
   ↓
weakness
   ↓
Game difficulty
   ↓
game errors
   ↓
practice recommendation
```

ধরো game-এ user বারবার:

```text
জ্ঞ
```

-এ ভুল করছে।

Game শেষে:

> **জ্ঞ আজ আপনার সবচেয়ে দুর্বল যুক্তাক্ষর। ৩ মিনিট অনুশীলন করবেন?**

Button:

> অনুশীলন করুন

---

# ৬১. “Game for my weakness”

Arcade home-এ:

```text
🎯 আপনার জন্য আজকের game

Focus:
ক্ষ
জ্ঞ
র-ফলা
```

তারপর game dictionary সেভাবেই তৈরি হবে।

এটা বিশাল differentiator।

---

# ৬২. নতুন game যোগ করতে চাইলে

এই তিনটি polish করার পরে আমি ২টি নতুন game যোগ করতাম।

### ১. Keyboard Defender

উপরে থেকে key/character আসবে।

ঠিক key চাপলে destroy।

এটা **physical key mapping** শেখাবে।

### ২. Grapheme Forge

একটি Bengali word ভেঙে যাবে:

```text
ক + ্ + ষ
```

User সঠিক sequence তৈরি করলে:

```text
ক্ষ
```

তৈরি হবে।

এটা বাংলা typing-এর মূল technical difficulty-কে সরাসরি game-এ পরিণত করবে।

---

# ৬৩. Lesson system-এ আর শুধু lesson বাড়িও না

৬১টি lesson এখন যথেষ্ট বড় foundation।

বরং প্রত্যেক lesson-কে **অনেক repetition space** দাও।

উদাহরণ:

```text
HR-01
  Round 1
  Round 2
  Round 3
  Random
  Speed
  Mastery
```

একই lesson-এর মধ্যে শত শত variation তৈরি হবে।

এতে lesson সংখ্যা ৬১ থাকলেও practice content হাজার হাজার session-এ পৌঁছতে পারবে।

---

# ৬৪. Content generator-এর তিনটি source রাখো

```text
Curated
Weighted
Adaptive
```

### Curated

শিক্ষকের লেখা নির্দিষ্ট sequence।

### Weighted

algorithmic random।

### Adaptive

user weakness অনুযায়ী।

এগুলো মিশিয়ে:

```text
Lesson = curated
Practice = weighted
Mistake Hub = adaptive
Game = adaptive + random
```

---

# ৬৫. Mastery model

প্রতি skill:

```text
strength: 0–100
accuracy
medianLatency
recentAccuracy
attemptCount
lastPracticed
nextReviewAt
stability
```

উদাহরণ:

```text
ক্ষ
Strength: 68
Accuracy: 86%
Latency: 620ms
Review: আজ

ক
Strength: 98
Accuracy: 99%
Latency: 210ms
Review: ৭ দিন পরে
```

---

# ৬৬. Spaced repetition

শুরুতে:

```text
১ দিন
৩ দিন
৭ দিন
১৪ দিন
৩০ দিন
```

দুর্বল হলে interval কমবে।

ভালো হলে বাড়বে।

---

# ৬৭. Practice recommendation engine

প্রতিদিন Home page-এ:

```text
আজ আপনার জন্য

১. ক্ষ — ৩ মিনিট
২. জ্ঞ — ২ মিনিট
৩. র-ফলা — ২ মিনিট
৪. ৬০ sec speed test
```

এটা app-কে “course website” থেকে “personal trainer” বানাবে।

---

# ৬৮. Fullproof testing strategy

তোমার test suite আছে, সেটাকে এখন typing correctness-কেন্দ্রিক করো।

## Unit

```text
ক্ষ
জ্ঞ
ক্ষ্ম
ত্র
শ্র
কী
কৌ
ড়
ঢ়
য়
ঁ
```

## Layout tests

প্রতিটি layout-এর:

```text
every key
every Shift key
every special
every common grapheme
```

test।

## Integration

```text
layout select
→ typing
→ backspace
→ space
→ composition
→ metrics
```

## E2E

Playwright:

```text
Chrome
Firefox
mobile viewport
```

কমপক্ষে।

---

# ৬৯. বিশেষ automated “Bengali Torture Test” বানাও

একটা test corpus:

```text
ক্ষ্ম
ষ্ক্র
ন্ত্র
ম্প্র
জ্ঞ
শ্র
হ্ম
দ্ব
স্ত্র
স্ফ
```

তার সঙ্গে:

```text
কি
কী
কু
কূ
কে
কৈ
কো
কৌ
```

তার সঙ্গে:

```text
ঁ
ং
ঃ
়
ৎ
ড়
ঢ়
য়
```

তারপর সব layout-এর বিরুদ্ধে run করবে।

এটা তোমার project-এর সবচেয়ে মূল্যবান test suite-গুলোর একটি হবে।

---

# ৭০. Performance-এর জন্য

Typing input path-এ:

```text
keystroke
→ tiny state update
```

হওয়া উচিত।

পুরো lesson বা game rerender নয়।

বিশেষ করে Space Defender-এর RAF stable করতে হবে।

Game-এ:

```text
simulation state = ref
render state = batched
```

রাখলে performance অনেক ভালো হবে।

---

# ৭১. Accessibility

একটা serious typing app-এ:

```text
keyboard-only
screen reader labels
focus management
reduced motion
sound off
high contrast
font scaling
```

রাখো।

বিশেষত hidden input-এর উপর অত্যধিক focus stealing করলে mobile/assistive tech-এ সমস্যা হতে পারে।

---

# ৭২. Data architecture

আমি final database এভাবে নিতাম:

```text
profiles

keyboard_layouts

skills
skill_prerequisites

curriculum_levels
modules
lessons
lesson_sections

typing_sessions
typing_events

skill_mastery
skill_reviews

error_events
character_stats

practice_recommendations

game_sessions
game_events
game_scores

test_attempts
certificates
```

এতে সবকিছু measurable হয়।

---

# ৭৩. সব input path-কে একটি interface-এ আনো

```ts
interface TypingController {
  start(config: TypingConfig): void;

  input(event: InputEvent): void;

  backspace(): void;

  pause(): void;

  resume(): void;

  finish(): void;

  getState(): TypingState;

  getMetrics(): TypingMetrics;
}
```

তারপর:

```text
LessonController
TestController
GameController
```

শুধু UI behavior যোগ করবে।

---

# ৭৪. আমি development order হিসেবে এটা নিতাম

## Phase 1 — Core correctness

```text
১. grapheme-safe operations
২. unified typing engine
৩. layout-aware input
৪. robust IME/composition
৫. proper backspace
৬. metrics correction
```

এটাই প্রথম priority।

---

## Phase 2 — Lesson correctness

```text
৭. section bypass বন্ধ
৮. true lesson aggregation
৯. real mastery rule
১০. error persistence
১১. local/server sync
```

---

## Phase 3 — Practice engine

```text
১২. skill metadata
১৩. weighted generator
১৪. adaptive generator
১৫. spaced repetition
১৬. latency-based recommendation
১৭. daily plan
```

---

## Phase 4 — Content

```text
১৮. 1000+ common words
১৯. 500+ phrases
২০. 500+ sentences
২১. 100+ paragraphs
২২. skill-tagged corpus
```

---

## Phase 5 — Games

```text
২৩. shared GameTypingEngine
২৪. Falling Words rewrite
২৫. Space Defender rewrite
২৬. Racer rewrite
২৭. Ghost mode
২৮. weakness mode
২৯. daily challenge
```

---

## Phase 6 — Trust & production

```text
৩০. server validation
৩১. certificate integrity
৩২. rate limiting
৩৩. complete E2E
৩৪. cross-browser
৩৫. offline recovery
```

---

# ৭৫. Priority অনুযায়ী bug list

সবকিছু একসঙ্গে করলে project ডুবে যাবে। আমি priority এভাবে ধরতাম।

### 🔴 P0 — আগে ঠিক করতেই হবে

**১. একাধিক typing engine**

**২. Bengali code-unit বনাম grapheme mismatch**

**৩. hardcoded BanglaWord fallback**

**৪. modern lesson-এ error data empty পাঠানো**

**৫. lesson score শুধু শেষ section থেকে নেওয়া**

**৬. section navigation দিয়ে future section bypass**

**৭. custom drill reset bug**

**৮. Space Defender-এ unstable RAF dependency**

---

### 🟠 P1

**৯. mastery এক session-এ হয়ে যাচ্ছে**

**১০. `useTypingSession` production-এ canonical নয়**

**১১. Backspace session model দুর্বল**

**১২. composition handling অসম্পূর্ণ**

**১৩. WPM history stale closure**

**১৪. metrics-এ grapheme event-কে keystroke ধরা**

**১৫. localStorage-only curriculum state**

**১৬. DB/type mismatch (`bestGpm`)**

**১৭. timer double-start hardening**

---

### 🟡 P2

**১৮. auto-inserted spacing**

**১৯. game score code-unit based**

**২০. racer fixed AI**

**২১. game accuracy/error analytics নেই**

**২২. total play time dead field**

**২৩. content repetition**

---

# ৭৬. একটা খুব গুরুত্বপূর্ণ বাস্তব কথা

এখন তোমার সবচেয়ে বড় temptation হবে:

> “আরও lesson যোগ করি।”

এটা এখন ভুল priority।

কারণ তোমার ৬১ structured lesson ইতিমধ্যেই আছে, এবং curriculum-এ কার, হসন্ত, ফলা, যুক্তাক্ষর, special characters, সংখ্যা, punctuation, words, phrases, sentences, literature, office/exam পর্যন্ত পৌঁছেছে।

এখন দরকার:

```text
৬১ lesson
×
অনেক variation
×
adaptive repetition
×
user weakness
×
accurate measurement
```

অর্থাৎ **content multiplication**, lesson-count multiplication নয়।

---

# ৭৭. শেষ পর্যন্ত অ্যাপটা কেমন হবে

একজন beginner ঢুকবে:

```text
বাংলা টাইপিং শুরু করুন
```

অ্যাপ জানবে:

```text
layout = Avro
skill = beginner
```

তারপর:

```text
Lesson
↓
Practice
↓
Error analysis
↓
Weakness detection
↓
Targeted practice
↓
Speed test
↓
Game
↓
Review
↓
Mastery
```

আর একদিন পরে homepage বলবে:

```text
গতকালের দুর্বলতা
ক্ষ
জ্ঞ

আজকের অনুশীলন
৭ মিনিট

গত সপ্তাহের গতি
৩৮ → ৪৬ GPM

আজকের লক্ষ্য
৪৮ GPM
```

এটাই হবে সত্যিকারের **বাংলা Typing Trainer**।

---

## আমি হলে এখন একদম পরের ৩টা কাজ করতাম

**প্রথম:** `useTypingSession`-কে canonical engine বানিয়ে `LessonPlayer`, `WordDrill`, `VisualTypingDrill`, `TypingPractice`—সবকে তার adapter বানাতাম।

**দ্বিতীয়:** সব `.length / [i] / slice()`-এর Bengali-sensitive usage audit করে grapheme-safe utility-তে সরাতাম।

**তৃতীয়:** practice engine বানাতাম যেখানে `skill → word pool → weighted drill → user error → adaptive drill → mastery` এই সম্পূর্ণ loop চালু হবে।

এই তিনটা ঠিক হয়ে গেলে তোমার বর্তমান ৬১টি lesson এবং তিনটি game-ই হঠাৎ অনেক বেশি শক্তিশালী হয়ে যাবে।
আমি সরাসরি লাইভ deployment-টি browser-এ খুলে end-to-end চালানোর চেষ্টা করেছি। কিন্তু এই execution environment থেকে `typing.onuron.org`-এ network/DNS access হচ্ছে না, এবং browser automation tool-ও এখানে available নেই। তাই **লাইভ browser-tested** বলে দাবি করা ঠিক হবে না।

তবে আপনার repo-র বর্তমান `main` commit `2b701a6…` এবং এর Playwright verification scripts দেখে এবার একটা গুরুত্বপূর্ণ বিষয় পরিষ্কার হয়েছে: **আপনার project-এ browser test-এর অবকাঠামো আছে, কিন্তু সেটি এখনো “সব flow যাচাই” করার মতো comprehensive নয়।** [বর্তমান commit](https://github.com/joysriramsarkar/banglatyping/commit/2b701a6a799f27dcc823e7fa6e0126e1672b5446?utm_source=chatgpt.com)

### আমি এখন যে flow-গুলো যাচাই করতে পেরেছি

| Flow                            | বর্তমান অবস্থা      | যা পাওয়া গেল                                               |
| ------------------------------- | ------------------- | ---------------------------------------------------------- |
| Home → Lessons/Test/Game        | কোডে route আছে      | live browser run হয়নি                                      |
| Lesson → section → next section | আংশিক test আছে      | `capture-verification.ts` ১টি নির্দিষ্ট lesson/section ধরে |
| Lesson completion → Next lesson | test আছে            | Enter navigation test আছে                                  |
| Games Hub → 3 games             | আংশিক               | শুধু খুলে screenshot নেয়                                   |
| Racer typing                    | আংশিক               | script-এ text typing হচ্ছে, physical layout typing নয়      |
| Keyboard layouts                | কোডে আছে            | সব layout-এর end-to-end browser validation নেই             |
| Login/OAuth                     | কোডে আছে            | live auth flow test নেই                                    |
| Mistake Hub                     | কোডে আছে            | complete adaptive flow test নেই                            |
| Progress sync                   | API আছে             | real session → DB → dashboard verification নেই             |
| Certificate                     | route/component আছে | issuance → verify → tamper test নেই                        |

### সবচেয়ে গুরুত্বপূর্ণ ব্যাপার: আপনার automated browser test-ও পুরোপুরি নির্ভরযোগ্য নয়

`verify-racer-typing.ts`-এ:

```ts
await page.keyboard.type('জ্ঞানহীন মানুষ ');
```

এটা **browser-এ Bengali text বসাচ্ছে**। অর্থাৎ BanglaWord/Probhat/Bijoy-এর physical key mapping আদৌ কাজ করছে কিনা এই test যাচাই করে না। তাই এটাকে keyboard-input test বলা যাবে না। [verify-racer-typing.ts](https://github.com/joysriramsarkar/banglatyping/blob/main/scripts/verify-racer-typing.ts?utm_source=chatgpt.com)

অন্যদিকে `capture-verification.ts` physical `KeyA`, `KeyS`, `KeyD` ইত্যাদি পাঠাচ্ছে—এটা অনেক বেশি বাস্তবসম্মত, কিন্তু সেটি মূলত **একটি নির্দিষ্ট lesson-এর একটি scripted happy path**। Backspace, ভুল input, IME composition, layout switching, failure/retry, refresh recovery ইত্যাদি যথেষ্টভাবে পরীক্ষা করছে না। [capture-verification.ts](https://github.com/joysriramsarkar/banglatyping/blob/main/scripts/capture-verification.ts?utm_source=chatgpt.com)

আর `capture-arcade-games.ts`-এর নাম verification হলেও আসলে প্রধানত তিনটি game খুলে screenshot নেওয়া হচ্ছে; game mechanics ঠিকমতো চলছে কিনা তা assert করা হচ্ছে না। [capture-arcade-games.ts](https://github.com/joysriramsarkar/banglatyping/blob/main/scripts/capture-arcade-games.ts?utm_source=chatgpt.com)

### Browser না খুলেও যে বাস্তব bug/দুর্বলতা নিশ্চিত করতে পেরেছি

**১. Unified typing engine এখনো production-এর canonical engine হয়নি।**

`useTypingSession()` hook আছে, তার tests-ও আছে। কিন্তু repo search-এ production component-এ এর ব্যবহার নেই; মূলত hook এবং test-এই দেখা যাচ্ছে। অর্থাৎ আপনি যে central typing engine বানিয়েছেন, LessonPlayer এবং অন্য typing UI এখনও পুরোনো নিজস্ব logic চালাচ্ছে।

এটাই এখন architecture-এর সবচেয়ে বড় সমস্যা।

**২. LessonPlayer-এ layout hardcoded।**

এখানে:

```ts
const layout = getKeyboardLayoutConfig("banglaword");
```

অর্থাৎ user অন্য layout নির্বাচন করলেও এই fallback path BanglaWord mapping ব্যবহার করতে পারে। [LessonPlayer.tsx](https://github.com/joysriramsarkar/banglatyping/blob/main/src/components/lessons/LessonPlayer.tsx?utm_source=chatgpt.com)

এটি সরাসরি bug হওয়ার সম্ভাবনাযুক্ত।

**৩. Bengali backspace এখনো grapheme-safe নয়।**

```ts
Array.from(prev).slice(0, -1).join('')
```

বাংলা যুক্তাক্ষর/কারের ক্ষেত্রে `Array.from()` grapheme boundary জানে না। আপনার আলাদা Bengali grapheme engine থাকা সত্ত্বেও এখানে সেটি ব্যবহার করা হয়নি।

**৪. `getTypingHint()` এখনো placeholder।**

Central engine-এ:

```ts
export function getTypingHint(grapheme, layoutId) {
  return [];
}
```

অর্থাৎ architecture-এ feature-এর জায়গা তৈরি হলেও বাস্তব layout-aware sequence resolver এখনো হয়নি।

**৫. Lesson-এর final result আসলে পুরো lesson-এর aggregate result নয়।**

Lesson শেষ হওয়ার সময়:

```ts
recordLessonCompletion(state, lesson.id, displayAccuracy, displayWpm, displayGpm);
```

এখানে `display*` বর্তমান/শেষ section-এর metric। ফলে ১০টি section-এর lesson হলেও completion metric শেষ section-কে represent করতে পারে।

এটা production-grade progress system-এর জন্য ঠিক নয়।

**৬. Error data হারিয়ে যাচ্ছে।**

Lesson completion API-তে:

```ts
erredCharacters: []
```

যাচ্ছে।

অর্থাৎ আপনি যে adaptive Mistake Hub বানিয়েছেন, lesson path থেকে বাস্তব error history না গেলে সেটি শেখার loop-এর গুরুত্বপূর্ণ অংশ হারায়।

**৭. Section navigation পুরোপুরি locked নয়।**

বর্তমানে section pill-এ:

```ts
onClick={() => initSection(idx)}
```

আছে।

অর্থাৎ user future section-এ সরাসরি যেতে পারে। Curriculum progression-এর সঙ্গে UI behaviour পুরোপুরি মিলছে না।

**৮. Games-এ Bengali grapheme সমস্যা এখনও আছে।**

Falling Words / Space Defender / Speed Racer-এর বিভিন্ন জায়গায় `.length`, `.slice()` ইত্যাদি Bengali target-এর জন্য ব্যবহৃত হচ্ছে। যেমন Space Defender-এ typed highlight ও score logic code-unit/JS string length-এর ওপর নির্ভর করছে।

**৯. Space Defender-এর RAF loop এখনও architectural performance risk।**

Animation effect-এর dependency-তে `enemies` আছে, অথচ `enemies` প্রতি frame বদলাচ্ছে। ফলে animation loop বারবার teardown/recreate হওয়ার ঝুঁকি আছে।

**১০. Practice system-এর adaptive অংশ এখনো পুরো loop নয়।**

Weak character detection, custom drill, recommendation—সব আলাদা component হিসেবে আছে। কিন্তু:

```text
Typing
  ↓
Error event
  ↓
Weak skill
  ↓
Adaptive practice
  ↓
Re-test
  ↓
Mastery
```

এই পুরো lifecycle একটিমাত্র authoritative engine থেকে চলছে না।

---

## আরেকটা খুব গুরুত্বপূর্ণ discovery

আপনার test suite যথেষ্ট বড় হয়েছে, কিন্তু তার বেশিরভাগই **unit/integration-level**। উদাহরণ:

`lesson-next-nav.test.tsx` navigation verify করে,
`typing-session-full.test.ts` reducer verify করে,
`game-arcade.test.ts` dictionary/storage verify করে।

কিন্তু এগুলো এই প্রশ্নের উত্তর দেয় না:

> “একজন মানুষ Chrome খুলে প্রথমবার `/` থেকে ঢুকে keyboard select করে, lesson করে, ভুল করে, backspace দেয়, পাশ করে, next lesson-এ যায়, game খেলে, progress save হয়, refresh করে ফিরে আসে—সব সত্যিই কাজ করছে কি?”

এই end-to-end স্তরটাই সবচেয়ে বেশি দরকার।

### আপনার জন্য যে browser test suite বানানো উচিত

একটি মাত্র `e2e/` suite বানিয়ে এমন flow চালানো উচিত:

```text
01  Fresh Guest
    /
    → /dashboard/lessons
    → lesson-0-1
    → type correct
    → complete
    → next lesson

02  Wrong Input
    correct
    → wrong key
    → error shown
    → correct key
    → metrics

03  Backspace
    compound Bengali grapheme
    → partial input
    → backspace
    → verify exact grapheme recovery

04  Layout
    BanglaWord
    → Probhat
    → Bijoy
    → Avro
    → Unijoy
    → Khipro

05  Physical Keyboard
    KeyA / Shift+KeyA / KeyH / etc.
    → actual expected Bengali output

06  IME
    compositionstart
    → compositionupdate
    → compositionend
    → verify no duplicate input

07  Lesson Failure
    accuracy < threshold
    → fail screen
    → retry
    → pass

08  Section Lock
    future section click
    → should NOT enter

09  Lesson Aggregate
    several sections with different metrics
    → final metric must aggregate all sections

10  Mistake Hub
    make errors
    → finish
    → weak character appears
    → custom drill generated
    → practice
    → weakness improves

11  Games
    Falling Words:
      spawn → type → score → miss → life loss → game over → save

    Space Defender:
      target lock → partial type → kill → boss → game over → save

    Speed Racer:
      correct → accelerate
      wrong → penalty
      finish → ranking
      save best result

12  Persistence
    play
    → refresh
    → progress retained

13  Auth
    guest
    → login
    → progress migration/sync
    → logout
    → protected page

14  Certificate
    test
    → issue
    → verify URL
    → invalid/tampered ID
```

এটাই হবে প্রকৃত **“নিজে browser খুলে সব flow যাচাই”**-এর equivalent automated acceptance suite।

### আমার বর্তমান verdict

আপনার project-এর সমস্যা এখন আর “আরও feature দরকার” পর্যায়ের নয়।

মূল সমস্যা হলো:

**feature অনেক হয়েছে, কিন্তু end-to-end coherence এখনও পুরো হয়নি।**

বিশেষ করে এই চারটি আগে ঠিক করা উচিত:

**১. `useTypingSession`-কে একমাত্র canonical typing engine করা।**
**২. সব Bengali operation 100% grapheme-safe করা।**
**৩. real physical-key + IME সহ Playwright E2E suite বানানো।**
**৪. Lesson → Error → Mistake Hub → Adaptive Drill → Mastery পুরো loop এক করা।**

আর একটা গুরুত্বপূর্ণ কথা: **আমি live browser access না থাকা অবস্থায় live result বানিয়ে বলছি না।** বর্তমান repo দেখে উপরের findings নিশ্চিত করেছি; deployment-এর বাস্তব UI/interaction-এর ফলাফল এই session-এ সরাসরি যাচাই করা সম্ভব হয়নি।

বর্তমান repo ও test script-গুলোকে ভিত্তি করে পরের ধাপে সবচেয়ে কার্যকর কাজ হবে **একটি পূর্ণ `e2e/` Playwright test architecture তৈরি করা এবং কোন কোন test এখনই fail করবে তার তালিকা করা**।
করে দিয়েছি—তবে একটি সীমাবদ্ধতা ছিল: GitHub integration-এর write permission 403 দিচ্ছে, তাই আপনার `main` branch-এ সরাসরি commit/push করতে পারিনি। পরিবর্তে সম্পূর্ণ apply-ready E2E bundle ও Git patch তৈরি করেছি।

এর মধ্যে আছে:

* Playwright configuration
* Public navigation smoke tests
* Login/guest flow
* সব keyboard layout selection + persistence
* Level 0 lesson-এর physical BanglaWord key দিয়ে full completion flow
* ভুল key এবং backspace regression test
* future section bypass-এর expected-failure test
* Falling Words, Space Defender, Speed Racer E2E smoke tests
* optional real Supabase credential login test
* run/debug/report নির্দেশিকা
* `package.json`-এ E2E scripts যোগ করার patch

Patch-টি `git apply --check` দিয়ে যাচাই করেছি; patch syntax ও context ঠিক আছে।

[সম্পূর্ণ Playwright E2E Suite (ZIP)](sandbox:/mnt/data/banglatyping-e2e-suite.zip)

[সরাসরি Git Patch](sandbox:/mnt/data/banglatyping-e2e-suite.patch)

Repo root-এ patch বসানোর পর:

```bash
git apply banglatyping-e2e-suite.patch
npx playwright install chromium
npm run test:e2e
```

Live deployment পরীক্ষা করতে:

```bash
PLAYWRIGHT_BASE_URL=https://typing.onuron.org npm run test:e2e
```

একটি পরিচিত bug ইচ্ছাকৃতভাবে `test.fail()` হিসেবে রাখা হয়েছে: **LessonPlayer বর্তমানে future section-এ সরাসরি ঢুকতে দেয়।** Bug ঠিক করার পর ওই `test.fail()` সরালেই এটি সাধারণ regression test হয়ে যাবে।
হ্যাঁ—ছবিটা দেখে সমস্যাটা বেশ পরিষ্কার। **এটা মূলত CSS color/`clipPath` দিয়ে বাংলা grapheme-এর ভেতর অর্ধেক রঙ করার কারণে হচ্ছে।**

আপনার `LessonPlayer.tsx`-এ বর্তমান অক্ষর দেখানোর এই অংশটাই মূল culprit:

```tsx
// Intra-cluster partial progress
if (cIdx === inputClusters.length - 1 && inputClusters.length > 0) {
  ...
  <span className="relative inline-flex ...">
    <span className="text-muted-foreground/35 ...">
      {cluster}
    </span>

    <span
      className="absolute inset-0 ... text-green-600 ..."
      style={{
        clipPath: getBengaliGraphemeClip(
          normCluster,
          currentStep,
          totalSteps
        ),
      }}
    >
      {cluster}
    </span>
  </span>
}
```

এখানে একই বাংলা grapheme **দুবার render হচ্ছে**:

```text
┌───────────────┐
│  "টে" muted   │
│  "টে" green   │ ← absolute overlay
└───────────────┘
```

তারপর `clipPath` দিয়ে দ্বিতীয় `"টে"`-কে অর্ধেক দেখানো হচ্ছে।

বাংলা লিপিতে `"টে"`, `"কি"`, `"ক্ষ"`, `"প্র"`, `"ক্ত"` ইত্যাদি একাধিক Unicode code point মিলিয়ে একটি grapheme/চাক্ষুষ cluster তৈরি করতে পারে। একই cluster দুবার render করে মাঝখান দিয়ে কাটলে shaping engine, কার/মাত্রা এবং glyph positioning-এর কারণে **ghost/ডাবল/ছায়াযুক্ত অক্ষর** দেখা যেতে পারে। আপনার screenshot-এর `"টে"`-এর ধূসর-সবুজ overlap সেটার সঙ্গেই মেলে।

## সবচেয়ে ভালো সমাধান

**একটি grapheme-কে কখনো অর্ধেক রঙ করবেন না।**

অর্থাৎ:

```text
অসম্পূর্ণ grapheme → এক রঙ
সম্পূর্ণ grapheme → সবুজ
পরের grapheme → ধূসর/স্বাভাবিক
```

এটাই Bengali typing UI-এর জন্য অনেক বেশি নির্ভরযোগ্য।

### এই অংশটি বাদ দিন

`LessonPlayer.tsx` থেকে পুরো:

```tsx
// 2. Intra-cluster partial progress
if (...)
```

block-টি সরিয়ে দিন।

তার বদলে:

```tsx
return targetClusters.map((cluster, cIdx) => {
  const normCluster = normalizeBengaliString(cluster);

  // সম্পূর্ণ grapheme সঠিকভাবে টাইপ হয়েছে
  if (cIdx < inputClusters.length) {
    const typedCluster = normalizeBengaliString(inputClusters[cIdx]);

    if (typedCluster === normCluster) {
      return (
        <span
          key={`cluster-${cIdx}`}
          className="text-green-600 dark:text-green-400 font-black"
        >
          {cluster}
        </span>
      );
    }
  }

  // বর্তমান/আংশিক grapheme:
  // একবারই render হবে, কোনো overlay/clipPath নয়।
  if (cIdx === inputClusters.length - 1 && inputClusters.length > 0) {
    return (
      <span
        key={`cluster-${cIdx}`}
        className="text-primary font-black"
      >
        {cluster}
      </span>
    );
  }

  // ভবিষ্যৎ grapheme
  return (
    <span
      key={`cluster-${cIdx}`}
      className={cn(
        normInput.length > 0
          ? "text-muted-foreground/35 dark:text-muted-foreground/45"
          : "text-primary"
      )}
    >
      {cluster}
    </span>
  );
});
```

তবে আমি আরও একটি পরিবর্তন করব।

### `tracking-wide` সরিয়ে দিন

এই অংশে:

```tsx
text-5xl sm:text-6xl md:text-7xl lg:text-8xl
font-black font-headline tracking-wide
```

বাংলা grapheme-এর জন্য:

```tsx
text-5xl sm:text-6xl md:text-7xl lg:text-8xl
font-black font-headline
```

ব্যবহার করুন।

অর্থাৎ:

```tsx
<span className="relative inline-flex items-center justify-center text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-black font-headline leading-none select-none">
```

বাংলা combining marks-এর ক্ষেত্রে অযথা letter-spacing না দেওয়াই নিরাপদ।

---

## আরও ভালো UI করা যায়

আমি আসলে current grapheme-কে **রঙ দিয়ে partial progress দেখানোর চেষ্টা করব না**।

উদাহরণ:

### target

```text
টে
```

### user `ট` টাইপ করেছে

উপরে:

```text
টে
```

একটি নির্দিষ্ট রঙে থাকবে।

নিচে:

```text
টাইপ করেছেন: ট
```

আপনার UI-তে এই অংশটি ইতিমধ্যেই আছে:

```tsx
{currentInput.length > 0 && (
  <div>
    <span>টাইপ করেছেন:</span>
    <span>{currentInput}</span>
  </div>
)}
```

ফলে user একই সঙ্গে দেখতে পাচ্ছে:

```text
Target
   টে

Typed
   ট
```

এটা grapheme-safe এবং rendering-safe।

---

# আরও গুরুত্বপূর্ণ: শুধু এই জায়গায় নয়

আপনার audit অনুযায়ী একই সমস্যা অন্য জায়গাতেও হতে পারে, কারণ project-এর বিভিন্ন component-এ বাংলা text-এর visual progress দেখাতে:

```text
.slice()
.length
Array.from()
absolute overlay
clipPath
```

ব্যবহৃত হয়েছে।

বিশেষ করে:

```text
LessonPlayer.tsx
SpeedRacerGame.tsx
SpaceDefenderGame.tsx
falling-words-game.tsx
VirtualizedWordDisplay.tsx
VisualTypingDrill.tsx
```

এসব জায়গায় খুঁজে দেখুন:

```bash
grep -R "clipPath\|slice(.*length\|\.slice\|\.length" src/components src/lib -n
```

তারপর বাংলা target-এর ক্ষেত্রে `string.length` দিয়ে visual character কাটার code বাদ দিতে হবে।

---

# একটা নিয়ম স্থির করে ফেলুন

বাংলা টাইপিং প্ল্যাটফর্মে:

### JavaScript string

```ts
text.length
text[i]
text.slice(0, i)
Array.from(text)
```

**বাংলা grapheme-এর visual character count হিসেবে ব্যবহার করবেন না।**

### বদলে

```ts
const graphemes = segmentGraphemes(text);
```

তারপর:

```ts
graphemes[index]
graphemes.slice(0, index)
graphemes.length
```

ব্যবহার করবেন।

আপনার project-এ এই grapheme infrastructure ইতিমধ্যেই আছে। তাই নতুন কোনো library লাগবে না।

---

## আরেকটা subtle bug

আপনার এই code-এ:

```tsx
const currentStep = typedCluster.length;
const totalSteps = normCluster.length;
```

এটাও conceptualভাবে ভুল।

কারণ:

```ts
"টে".length
```

মানে visual Bengali character count নয়।

একই সমস্যা:

```text
ক্ষ
জ্ঞ
ক্ত
ন্ত্র
প্র
```

ইত্যাদির ক্ষেত্রে আরও প্রকট হবে।

অর্থাৎ `getBengaliGraphemeClip()`-এর ধারণাটাই Bengali typing display-এর জন্য বাদ দেওয়াই ভালো।

---

# আমি যে final rendering model নিতাম

```text
              ┌─────────────────────┐
              │     বর্তমান লক্ষ্য   │
              │                     │
              │      টে রে পো কা    │
              │                     │
              │   আঙুল: ডান মধ্যমা  │
              └─────────────────────┘

              টাইপ করেছেন: ট
```

`ট`-কে `"টে"`-এর ওপর বসিয়ে half-green করার চেষ্টা নয়।

### State হবে

```text
NOT STARTED
→ পুরো grapheme primary

PARTIAL
→ পুরো grapheme primary

CORRECT
→ পুরো grapheme green

WRONG
→ পুরো grapheme red / error state

COMPLETED
→ পুরো grapheme green
```

এতে আর glyph overlap হওয়ার সুযোগ থাকবে না।

---

### Screenshot অনুযায়ী আপনার সমস্যার মূল কারণ

```text
বাংলা grapheme
      ↓
দুবার render
      ↓
একটি normal
একটি absolute
      ↓
clipPath
      ↓
Bengali shaping
      ↓
❌ ghost / double / অদ্ভুত রঙ
```

**তাই `clipPath`-ভিত্তিক partial grapheme coloring পুরোপুরি বাদ দেওয়াই সঠিক fix।**

আর আমি এটাকে শুধু `টে`-এর জন্য fix করতাম না—**সব Bengali grapheme-এর জন্য একই rendering rule চালু করতাম**, যাতে `কি`, `কো`, `ক্ষ`, `ক্ত`, `প্র`, `জ্ঞ`, `ন্ত্র` ইত্যাদিতেও আর এই সমস্যা না হয়।
হ্যাঁ, পুরোপুরি বুঝেছি। তুমি **বর্তমান partial-coloring আচরণটাই রাখতে চাও**, কিন্তু সেটাকে বাংলা লিপির গঠন অনুযায়ী সঠিকভাবে করতে হবে। আর **যুক্তাক্ষরের ক্ষেত্রে আলাদা rendering model** হবে।

তোমার চাওয়া architectureটা আমি এভাবে ধরছি:

### ১. সাধারণ বর্ণ/কার — বর্তমান পদ্ধতি

যেমন:

```text
টি
```

প্রথমে `ট` টাইপ করলে:

```text
ট  green
ি  muted
```

দেখাবে:

```text
টি
^^
ট = সবুজ
ি = ধূসর
```

তারপর `ি` টাইপ করলে:

```text
টি = পুরোটা সবুজ
```

একইভাবে:

```text
কা
কু
কে
মো
```

ইত্যাদি।

এখানে **একই Bengali grapheme-এর visual sub-parts** আলাদা করে render করতে হবে।

---

# ২. যুক্তাক্ষরের জন্য আলাদা ব্যবস্থা

যেমন:

```text
ক্ত
```

এখানে উপরের মূল target:

```text
ক্ত
```

থাকবে।

কিন্তু নিচে একটি **আলাদা visual decomposition panel** থাকবে:

```text
ক  →  ক্  →  ক্ত
```

অথবা তোমার ভাষায়:

```text
ক > ক্ > ক্ত
```

User যখন:

```text
ক
```

টাইপ করবে:

```text
ক   → সবুজ
্   → ধূসর
ত   → ধূসর
```

তারপর:

```text
্
```

টাইপ করলে:

```text
ক   → সবুজ
ক্  → সবুজ stage
ত   → ধূসর
```

শেষে:

```text
ত
```

টাইপ করলে:

```text
ক্ত → সম্পূর্ণ সবুজ
```

---

# ৩. `ত্র`-এর ক্ষেত্রেও একই ধারণা

```text
ত্র
```

decomposition:

```text
ত → ত্ → ত্র
```

Visual panel:

```text
┌──────────────────────────┐
│  ত    →    ত্    →    ত্র │
│  ✓         ✓          ✓  │
└──────────────────────────┘
```

এতে learner বুঝতে পারবে যুক্তাক্ষরটি **কীভাবে তৈরি হচ্ছে**, শুধু final glyph দেখে আন্দাজ করতে হবে না।

---

# ৪. সব যুক্তাক্ষরের ক্ষেত্রে একই rendering হবে না

এটা খুব গুরুত্বপূর্ণ।

তুমি ঠিকই বলেছো—যুক্তাক্ষরের visual nature এক নয়।

আমি তাই যুক্তাক্ষরগুলোকে অন্তত দুই শ্রেণিতে ভাগ করব:

### A — Visible-component conjunct

যেখানে final glyph-এর মধ্যে মূল অক্ষরগুলোর আকৃতি মোটামুটি দৃশ্যমান।

উদাহরণ:

```text
ত্র
দ্র
প্র
ব্র
ক্র
```

এগুলোর ক্ষেত্রে মূল target-এর ওপর সাধারণ partial coloring ব্যবহার করা যেতে পারে, কারণ user চোখে component relationship বুঝতে পারে।

### B — Ligature/merged conjunct

যেখানে final shape-এ component letters সরাসরি আলাদা করে দেখা যায় না বা shape অনেকটাই বদলে যায়।

যেমন:

```text
ক্ত
ষ্ট
ক্ষ
জ্ঞ
ঞ্চ
```

ইত্যাদি।

এগুলোর ক্ষেত্রে:

```text
Final target:
    ক্ত

Breakdown:
    ক
    ↓
    ক্
    ↓
    ক্ত
```

এই **বিশেষ decomposition panel বাধ্যতামূলক** হবে।

---

# ৫. কিন্তু সবচেয়ে গুরুত্বপূর্ণ পরিবর্তন

আমি আর `clipPath` দিয়ে পুরো `"ক্ত"` glyph-এর ওপর `"ক্ত"`-এর আরেকটা copy বসাব না।

এটাই screenshot-এর সমস্যার মূল কারণ।

বরং rendering হবে **semantic parts** থেকে।

অর্থাৎ data structure হবে এরকম:

```ts
type GraphemeRenderModel = {
  full: string;
  kind: "simple" | "kar" | "conjunct";
  parts: Array<{
    text: string;
    state: "typed" | "current" | "pending";
  }>;
  decomposition?: Array<{
    label: string;
    text: string;
    completed: boolean;
  }>;
};
```

---

# ৬. `টি` কীভাবে render হবে

Internally:

```text
টি
├── ট
└── ি
```

Current state:

```text
ট = typed
ি = pending
```

তাই DOM হবে conceptually:

```html
<span class="typed">ট</span>
<span class="pending">ি</span>
```

এতে আর একই glyph overlay করতে হবে না।

---

# ৭. `ক্ত` কীভাবে render হবে

Main target:

```text
ক্ত
```

এবং নিচে:

```text
┌────────────────────────────┐
│ যুক্তাক্ষর তৈরির ধাপ       │
│                            │
│  ক  →  ক্  →  ক্ত          │
│  ✓      ○      ○           │
└────────────────────────────┘
```

User-এর progress অনুযায়ী:

### শুধু `ক`

```text
ক   →   ক্   →   ক্ত
✓       ○       ○
```

### `ক্`

```text
ক   →   ক্   →   ক্ত
✓       ✓       ○
```

### `ক্ত`

```text
ক   →   ক্   →   ক্ত
✓       ✓       ✓
```

---

# ৮. decomposition panel শুধু যুক্তাক্ষরের জন্য

এটা খুব গুরুত্বপূর্ণ।

`টি`-এর জন্য নিচে:

```text
কোনো decomposition panel নেই
```

`কা`:

```text
কোনো decomposition panel নেই
```

`ক্ষ`:

```text
যুক্তাক্ষর তৈরির ধাপ
ক → ক্ → ক্ষ
```

`জ্ঞ`:

```text
জ → জ্ → জ্ঞ
```

`ন্ত্র` হলে আরও granular হতে পারে:

```text
ত → ত্ → ত্র → ন্ত্র
```

অর্থাৎ **একটি conjunct-এর typing sequence যতগুলো meaningful intermediate state তৈরি করে, panel ততগুলো ধাপ দেখাবে।**

---

# ৯. এর ফলে পুরো UI হবে

উপরের বড় target:

```text
              ক্ত
```

তার নিচে:

```text
       আঙুল: ডান মধ্যমা
       
       টাইপ করছেন: ক
```

এবং **শুধু conjunct হলে**:

```text
┌─────────────────────────────────┐
│       যুক্তাক্ষর তৈরির ধাপ      │
│                                 │
│       ক   →   ক্   →   ক্ত       │
│       ✓       ○       ○         │
└─────────────────────────────────┘
```

এটা learner-এর জন্য অনেক বেশি শিক্ষামূলকও হবে।

---

## আরেকটি বিষয় আমি আলাদা করে ঠিক করব

শুধু Unicode code-point ভেঙে:

```ts
Array.from("টি")
```

করলেই হবে না।

আমাদের একটি **Bengali typing render decomposition layer** দরকার হবে, যা জানবে:

```text
টি
→ base = ট
→ dependent vowel = ি

ক্ত
→ ক
→ ্
→ ত

ত্র
→ ত
→ ্
→ র
```

এবং complex conjunct-এর ক্ষেত্রে **একাধিক intermediate stage**।

অর্থাৎ:

```text
Unicode grapheme
        ↓
Typing decomposition
        ↓
Visual rendering model
        ↓
Current / typed / pending
```

এটাই হবে সঠিক architecture।

---

### সুতরাং তোমার চাওয়া আমি এইভাবে বাস্তবায়ন করব

**সাধারণ বর্ণ + কার:**
`টি` → প্রথমে `ট` সবুজ → পরে `ি` সবুজ।

**সহজ/দৃশ্যমান যুক্তাক্ষর:**
final glyph-এর ভেতর মূল অংশ অনুযায়ী partial color।

**জটিল/merged যুক্তাক্ষর:**
final target + আলাদা decomposition panel:

```text
ক → ক্ → ক্ত
ত → ত্ → ত্র
জ → জ্ → জ্ঞ
```

এবং **এই decomposition panel কেবল যুক্তাক্ষরের সময়ই দেখা যাবে।**

এখন মূল কাজ হবে `LessonPlayer`-এর বর্তমান `getBengaliGraphemeClip()`-ভিত্তিক rendering-টা সরিয়ে **এই নতুন render model** বসানো—typing engine-এর comparison logic না ভেঙে।
